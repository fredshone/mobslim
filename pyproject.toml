[project]
name = "mobslim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.4.0",
    "networkx>=3.5",
    "pandas>=2.3.1",
]
